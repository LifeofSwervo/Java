public class Tree
{
    private Node root;

    /**
     * - Constructor,
     */
    public Tree()
    {
        root = null;
    }

    /**
     * -
     * @param key - An integer. The key used to identify node.
     * @return -
     */
    public Node find(int key)
    {
        Node current = root;
        while
    }
}
